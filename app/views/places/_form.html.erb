  <h1>Add Place</h1>
<%= form_for @place do |form| %>
  <% if @place.errors.any? %>
  <div id="error_explanation">
  <h2>
  <%= pluralize(@place.errors.count, "error") %> prohibited this billing from being saved:
  </h2>
  <ul>
<% @place.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
   <% end %>
   </ul>
    </div>
<% end %>
    
<div class="field">
    <%= form.label :place_name %>
    <%= form.text_field :place_name%>
</div><br>

<div class="field">
    <%= form.label :node_name %>
    <%= form.text_field :node_name%>
</div><br>

<div class="field">
  <%= form.label :company_id %><br />
  <%= form.select :company_id, Company.all.collect { |a| [a.name, a.id ]  } %>
</div><br>


<%= form.submit"Save" %>
  
<% end %>